// Powerfull Array Methods
// forEach, map, filter, find, redunce
// Iterate over array - no for loop required
// Accept CallBack function as an argument, call CallBack against each item in a arra. reference Item in the Callback Paremater

const numbers = [0, 1, 2, 3, 4];

// show all numbers

for (let i = 0; i < numbers.length; i++){
    console.log(numbers[i]);
}